export const metadata = {
  title: 'Extractor de Facturas PDF',
  description: 'Extrae datos de facturas PDF usando GPT-4',
}

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  )
}
